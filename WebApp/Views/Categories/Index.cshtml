@using WebApp.Models;
@model List<Category>

@section title {
    <h3>Categories</h3>
}
<ul>
    @foreach(var category in Model)
    {
        <li>
            @category.Name
        </li>
    }
</ul>


